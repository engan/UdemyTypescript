module Utility {

    export class Useful {
        // timesTwo is a function, but word function removed 
        // since the function is inside a class
        timesTwo(n:number) { 
            return n * 2;
        }

    }

}